CREATED FUNCTIONS FOR DISK STREAMS

WORKDONE:-  
    using disk streams we can read any number of bytes from any location from anywhere in a disk, we have implemented a disk_stream.h as a header file and disk_stream.c for all prototype definition

NOTES:-
    -[notes on disk_stream.h](../disk_stream_h)
    -[notes on disk_stream.c](../disk_stream)

TOOLS:
    vim,qemu
